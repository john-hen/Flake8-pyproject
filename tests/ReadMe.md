## Test suite

The scripts here, along with the fixtures, constitute the test suite.
Run them with `pytest` from the root folder. Or use the scripts `run_tests.py`
and `measure_coverage.py` from the `tools` folder.
